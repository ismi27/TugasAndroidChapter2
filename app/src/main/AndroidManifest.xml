<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tugas4sc6ismi.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.tugas4sc6ismi.myapplication.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TableLayout" />
        <activity android:name=".CustomAdapter" />
        <activity android:name=".ListBuku2" />
        <activity android:name=".LinearLayout" />
        <activity android:name=".RelativeLayout2" />
        <activity android:name=".FrameLayout" />
        <activity android:name=".WebView" />

    </application>

</manifest>